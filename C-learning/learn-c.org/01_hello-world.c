#include <stdio.h>

int main() {
	// our code goes here.
	printf("Hello, World!");
	return 0; 
}

// every line in C ends with a semicolon ";", so that the compiler knows there is a new line started. 
	// If we want to say that our code was run successfully, we will return the number 0.
	// A number greater than 0 will mean that the program that we wrote failed.