#Binômio de Newton
n1 = int(input('Digite o n: '))
p = int(input('Digite o p: '))

def fat(n):
	fat(0) = 1
	num = 1 #definindo a variável, dentro da função
	while n >= 1:
		num *= n
		n -= 1 #update n
	if fat(1) == 1 and fat(2) == 2
	return(num)
	
np = (fat(n1) / fat(n1 - p) * fat(p)) 
print(np)

#função fat 1'
#def numero_binomial(n, k):
#	return (fatorial(n) / fatorial(k) * fatorial(n - k))
#====================
#note que retornará um valor fracionário, ou seja, TIPO FLOAT, pois
# não foi feita divisão inteira