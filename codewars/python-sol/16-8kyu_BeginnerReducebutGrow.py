def grow(arr):
	var = 1
	return (var *= i for i in arr)

'''def grow(arr):
	var = 1
	for i in arr:
		var *= i
	return var
'''
#Na linha 4 atualizamos var, 1 (que é o Elemento Neutro da Multiplicação), multiplicando cada
#índice da lista 'i' pela mesma.
#08/03