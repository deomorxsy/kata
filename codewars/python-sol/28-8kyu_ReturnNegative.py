def make_negative(number):
    # Retorne número * -1 (para o tornar negativo) se número for > 0.
    #Senão, apenas retorne-o.
    return (number * -1 if number > 0 else number)

'''
# Ou, da forma convencional:
    if number > 0:
    	return number * -1
    else:
    	return number

'''